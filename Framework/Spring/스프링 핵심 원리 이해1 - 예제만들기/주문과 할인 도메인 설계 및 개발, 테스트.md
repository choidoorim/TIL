- 인텔리제이에서 cmd + n 단축키에 toString 을 선택하면 객체를 보기 쉽게 만들어주는 메서드를 호출할 수 있다.

```java
public class FixDiscountPolicy implements DiscountPolicy {  
    private int discountFixAmount = 1000;  
  
    @Override  
    public int discount(Member member, int price) {  
        if (member.getGrade() == Grade.VIP) {  
            return discountFixAmount;  
        }  
        return 0;  
    }  
}

public class OrderServiceImpl implements OrderService {  
    private final MemberRepository memberRepository = new MemoryMemberRepository();  
    private final DiscountPolicy discountPolicy = new FixDiscountPolicy();  
  
    @Override  
    public Order createOrder(Long memberId, String itemName, int itemPrice) {  
        Member member = memberRepository.findById(memberId);  
  
        // 이 로직이 설계가 잘된 이유는 할인관련된 로직은 Order 자신이 관여하지 않아도 되기 때문이다.
        // 즉, 단일 책임 원칙을 잘 지킨 로직인 것이다.  
        discountPolicy.discount(member, itemPrice);  
        return null;    
    }  
}
```

- Type Long 과 long 의 차이점은 Long 은 Null 을 허용하지 않는다는 것이다.
